name: 'ChangeColour'
description: 'Change the colour of a square in the grid'
inputs:
  title:
    description: 'Github issue title'
    required: true
  user:
    description: 'Github user'
    required: true
runs:
  using: 'node16'
  main: 'app.js'